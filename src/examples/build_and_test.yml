description: >
  Build and publish nodejs projects to npm and docker.
usage:
  version: 2.1
  setup: true
  orbs:
    build: <namespace>/<orb-name>@1.0.15
  workflows:
    build_and_test:
      jobs:
        - build/workflow
